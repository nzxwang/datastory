library(tidyverse)

#2
# if a has m components and b has p components, a*b^T is a m*p matrix with rank 1.
# a_i*b_j is in row i, column j.
# a*a^T is a mxm square matrix with rank 1.

#6
# those rank one matrices are the projections of those vectors a_i onto the unit vectors b_i.

